const age: number = 34;

const days = age * 365;
const hours = days * 24;
const minutes = hours * 60;
const seconds = minutes * 3600;

//добавить минуты

console.log(
  `Мне ${days} дней \
${hours} часов \
${minutes} минут и \
${seconds} секунд`,
);
